@section Styles {
    <link rel="stylesheet" href="~/css/company.css"/>
}

<main class=" mx-auto px-36 py-16">

    <div class="text-center mb-16 font-semibold ">
        <h1 class="text-4xl mb-3 
               text-black dark:text-white transition-colors duration-300">
            Şirkətlər
        </h1>
        <p class="text-lg font-medium
              text-black dark:text-white transition-colors duration-300">
            Arzuladığınız iş yerini tapmaq üçün şirkətlərin siyahısı ilə tanış olun.
        </p>
    </div>

    <div class="flex flex-col lg:flex-row gap-10">
        <aside class="w-full lg:w-[300px] shrink-0">
            <div class="bg-white rounded-2xl border border-lightGreen p-6 sticky top-28 shadow-sm">
                <div class="relative mb-8">
                    <span class="absolute inset-y-0 left-4 flex items-center text-lightGreen">
                        <iconify-icon icon="ph:magnifying-glass-bold" class="text-xl"></iconify-icon>
                    </span>

                    <input type="text"
                           name="search"
                           value="@ViewBag.Search"
                           placeholder="Şirkət axtar..."
                           class="w-full py-3.5 pl-12 pr-4 bg-gray-50 border border-lightGreen  rounded-xl text-sm focus:outline-none focus:border-[#1A998D] transition-all"
                           hx-get="/companies"
                           hx-trigger="keyup changed delay:500ms, search"
                           hx-target="#main-content-area"
                           hx-push-url="true">
                </div>

                <div class="space-y-1">
                    @foreach (var company in Model)
                    {
                        <a href="#"
                           class="sidebar-link @(company.IsActive ? "sidebar-active" : "text-gray-500 hover:bg-gray-50") flex items-center px-5 py-3.5 rounded-xl text-md    font-bold transition-all">
                            @company.Name
                        </a>
                    }
                </div>
            </div>
        </aside>

        <div class="flex-1" id="main-content-area">

            <div class="sort-container">
                <div class="sort-select-box">
                    <select class="sort-select"
                            name="sort"
                            hx-get="/companies"
                            hx-target="#main-content-area"
                            hx-trigger="change">
                        <option value="newest">Ən yeni</option>
                        <option value="popular">Məşhur</option>
                    </select>
                    <iconify-icon icon="ph:caret-down-bold" class="sort-icon"></iconify-icon>
                </div>
            </div>

            <div id="companies-grid" class="flex flex-wrap gap-5">
                <partial name="_CompanyCard" model="(dynamic)Model" />
            </div>
        </div>
    </div>

    <div class="flex justify-center items-center gap-3 mt-24">
    
        @if (ViewBag.CurrentPage > 1)
        {
            <button hx-get="/companies?page=@(ViewBag.CurrentPage - 1)&search=@ViewBag.Search"
                    hx-target="#main-content-area"
                    hx-push-url="true"
                    class="px-7 py-3 border border-gray-200 rounded-xl text-gray-500 font-bold text-[14px] hover:border-[#1A998D] hover:text-[#1A998D] transition-all">
                Əvvəlki
            </button>
        }

        @for (int i = 1; i <= ViewBag.TotalPages; i++)
        {
            <button hx-get="/companies?page=@i&search=@ViewBag.Search"
                    hx-target="#main-content-area"
                    hx-push-url="true"
                    class="w-12 h-12 flex items-center justify-center rounded-xl font-bold text-[14px] transition-all
                @(i == ViewBag.CurrentPage
                   ? "bg-[#1A998D] text-white shadow-lg shadow-emerald-700/20"
                   : "border border-gray-200 text-gray-500 hover:border-[#1A998D] hover:text-[#1A998D]")">
                @i
            </button>
        }

        @if (ViewBag.CurrentPage < ViewBag.TotalPages)
        {
            <button hx-get="/companies?page=@(ViewBag.CurrentPage + 1)&search=@ViewBag.Search"
                    hx-target="#main-content-area"
                    hx-push-url="true"
                    class="px-7 py-3 border border-gray-200 rounded-xl text-gray-500 font-bold text-[14px] hover:border-[#1A998D] hover:text-[#1A998D] transition-all">
                Növbəti
            </button>
        }
    </div>
    
</main>